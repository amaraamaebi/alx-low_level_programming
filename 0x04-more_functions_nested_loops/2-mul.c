#include "main.h"
#include <stdio.h>

/**
 * mul - mul(int a, int b).
 * @a: variable collect.
 * @b: variable collect.
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int sum;

	sum = a * b;
	return sum;
}
