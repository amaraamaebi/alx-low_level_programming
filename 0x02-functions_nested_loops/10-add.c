#include <stdio.h>
#include "main.h"

/**
 * add - Entry point
 * @a: integer to add:
 * @b: integer to add
 * Return: 1 on (Success)
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
